@model ClinicaMedica.App.ViewModels.MedicoViewModel

@{
    ViewData["Title"] = "Detalhes do médico " + Model.Nome;
}

<div class="row">
    <h2>Detalhes do médico <span class="text-info">@Model.Nome</span></h2>
</div>

<div>
    <hr />
    
    <partial name="_DetailsDelete" />

</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a> &nbsp;
    <a asp-action="Index" class="btn btn-primary">Voltar</a>
</div>
